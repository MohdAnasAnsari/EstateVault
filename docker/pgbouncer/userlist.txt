# PgBouncer userlist — replace hash with real scram-sha-256 hash in production
# Generate with: psql -c "SELECT usename, passwd FROM pg_shadow WHERE usename='vault';"
"vault" "SCRAM-SHA-256$4096:CHANGEME"
