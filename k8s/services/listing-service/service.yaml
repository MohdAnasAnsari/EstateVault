apiVersion: v1
kind: Service
metadata:
  name: listing-service
  labels:
    app: vault
    service: listing-service
spec:
  selector:
    service: listing-service
  ports:
    - port: 3002
      targetPort: 3002
      name: http
  type: ClusterIP
