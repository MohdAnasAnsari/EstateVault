apiVersion: v1
kind: ServiceAccount
metadata:
  name: call-service-sa
  labels:
    app: vault
    service: call-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: call-service
  labels:
    app: vault
    service: call-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      service: call-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        service: call-service
        app: vault
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3005"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: call-service-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: call-service
          image: ghcr.io/YOUR_ORG/vault-call-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3005
              name: http
          envFrom:
            - configMapRef:
                name: call-service-config
            - secretRef:
                name: vault-secrets
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 5
            periodSeconds: 10
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - sleep 5
      terminationGracePeriodSeconds: 30
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              service: call-service
