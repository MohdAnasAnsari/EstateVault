apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: vault
    service: web
spec:
  selector:
    service: web
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
